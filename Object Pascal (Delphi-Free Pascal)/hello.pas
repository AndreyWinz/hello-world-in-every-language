program Project1;
{$APPTYPE CONSOLE} // Optional directive for console apps in Delphi GUI projects
begin
  writeln('Hello, World!');
  // readln; // Optional: keeps the console window open until a key is pressed
end.
